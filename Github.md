Esse é o primeiro projeto que estou criando no Github, então vou guardar alguns comandos aqui:

git add .
git status
git commit -m "nome da alteração"
git push origin main
git checkout -b "new-branch"
